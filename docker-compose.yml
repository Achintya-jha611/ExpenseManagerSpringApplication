services:
  springboot:
    depends_on:
      - mysql
    build: .
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:mysql://mysql:3306/expense_manager
      DB_PASSWORD: testDockerCompose123
      DB_USER: root
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: expense_manager
      MYSQL_ROOT_PASSWORD: testDockerCompose123

